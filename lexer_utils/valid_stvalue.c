
/*
 * * autor: roberto-xz 10, Junho 2025
 **
*/

#include <string.h>
#include <string.h>
#include <stdlib.h>

short valid_stvalue(char *value) {
    if (value == NULL) return 1;
    if (strlen(value) < 6 || strlen(value) > 6) return 1;

    // converter o hex para dec, e verificar se está no limite de intervalo
    // permitido.

    return 0;
}
